[package]
name = "tish_broker"
version = "0.1.0"
edition = "2021"
description = "Tauri-free BrokerCore — SharedState, surfaces, invoke dispatch (app hosts; not the Tish language)"
license = "MIT OR Apache-2.0"
repository = "https://github.com/tishlang/tish-desktop"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
parking_lot = "0.12"
once_cell = "1"
