# tish_broker

Tauri-free **BrokerCore** for Tish app hosts: shared state (`state.*`), surfaces, and invoke dispatch.

Used by `tish_desktop` (and optionally by sibling platform hosts via path dep in a monorepo checkout). Publishable standalone on crates.io.
