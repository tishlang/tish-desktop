[package]
name = "tishlang_app"
version = "0.1.0"
edition.workspace = true
description = "cargo:tishlang_app — alias re-export of tishlang_desktop (cross-device app runtime)"
license-file.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
keywords = ["tish", "desktop", "app"]
categories = ["gui", "development-tools"]

[lib]
name = "tishlang_app"
crate-type = ["rlib"]

[features]
default = ["webview-tauri"]
webview-tauri = ["tishlang_desktop/webview-tauri"]
platform-apple = ["tishlang_desktop/platform-apple"]
platform-ms = ["tishlang_desktop/platform-ms"]
platform-lin = ["tishlang_desktop/platform-lin"]

[dependencies]
tishlang_desktop = { path = "../tish_desktop" }
