{
  "name": "tish-mode",
  "version": "0.1.0",
  "private": true,
  "description": "Tish Mode monorepo — app runtime workspace. Publishable packages live under @tishlang/tish-desktop* and crates tish_desktop / tish_broker / tish_app.",
  "license": "PIF",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tishlang/tish-desktop.git"
  },
  "bin": {
    "mode": "cli/bin/mode.js"
  },
  "scripts": {
    "check": "cargo check -p tish_desktop",
    "mode": "node cli/bin/mode.js",
    "format": "tish-format examples packages cli scripts 2>/dev/null || npx --yes @tishlang/tish-format examples packages cli",
    "format:check": "tish-format --check examples packages cli 2>/dev/null || npx --yes @tishlang/tish-format --check examples packages cli",
    "lint:tish": "tish-lint examples packages cli 2>/dev/null || npx --yes @tishlang/tish-lint examples packages cli",
    "lint:rust": "cargo fmt -p tish_desktop -- --check && cargo clippy -p tish_desktop -- -D warnings",
    "example:basic": "npm --prefix examples/basic install && npm --prefix examples/basic run dev",
    "example:basic:rebuild": "npm --prefix examples/basic install && npm --prefix examples/basic run dev -- --rebuild",
    "example:file-browser": "npm --prefix examples/file-browser install && npm --prefix examples/file-browser run dev",
    "example:file-browser:rebuild": "npm --prefix examples/file-browser install && npm --prefix examples/file-browser run dev -- --rebuild",
    "example:native-chrome": "npm --prefix examples/native-chrome install && npm --prefix examples/native-chrome run dev",
    "example:native-chrome:rebuild": "npm --prefix examples/native-chrome install && npm --prefix examples/native-chrome run dev -- --rebuild",
    "example:byo-ui": "npm --prefix examples/byo-ui install && npm --prefix examples/byo-ui run dev",
    "example:hybrid": "npm --prefix examples/hybrid install && npm --prefix examples/hybrid run dev",
    "example:hello-ios": "node cli/bin/mode.js ios --example hello-ios",
    "check:bare-deps": "node -e \"const p=require('./examples/byo-ui/package.json'); const d={...p.dependencies,...p.devDependencies}; for (const k of Object.keys(d)) { if (k==='lattish'||k==='@tishlang/tish-desktop-ui-theme') { console.error('bare example must not depend on',k); process.exit(1);} } console.log('byo-ui: no lattish/ui-theme');\"",
    "build:basic": "npm --prefix examples/basic install && npm --prefix examples/basic run build:ui && npm --prefix examples/basic run build:shell",
    "build:file-browser": "npm --prefix examples/file-browser install && npm --prefix examples/file-browser run build:ui && npm --prefix examples/file-browser run build:shell",
    "build:native-chrome": "npm --prefix examples/native-chrome install && npm --prefix examples/native-chrome run build:ui && npm --prefix examples/native-chrome run build:shell",
    "distribute:build": "node scripts/distribute/build-release.mjs",
    "distribute:sign": "bash scripts/distribute/sign-macos.sh",
    "distribute:notarize": "bash scripts/distribute/notarize-macos.sh",
    "distribute:updater": "node scripts/distribute/publish-updater.mjs",
    "distribute:release": "node scripts/distribute/build-release.mjs && node scripts/distribute/publish-github-release.mjs"
  },
  "devDependencies": {
    "@tishlang/tish-format": "^2.39.0",
    "@tishlang/tish-lint": "^2.39.0"
  }
}
