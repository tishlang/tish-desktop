// AppKit Button — label is children text (host reads children, not title=).
export fn Button(props) {
  let title = props.title === null ? "Button" : props.title
  return <button onClick={props.onPress}>{title}</button>
}
