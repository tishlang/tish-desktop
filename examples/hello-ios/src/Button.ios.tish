// iOS-native Button (cascade: .ios → .native → .tish)

export fn Button(props) {
  let label = props.label !== null ? props.label : "Button"
  let onPress = props.onPress
  return (
    <button onClick={() => {
      if (onPress) {
        onPress()
      }
    }}>{"ios:" + label}</button>
  )
}
